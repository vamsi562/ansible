- name: Conditions
  hosts: local
  connection: local
  vars:
    number: 5
  tasks:
  - name: condition check
    ansible.builtin.debug:
      msg: "Given number {{ number }} less than 10"
    when: number<10

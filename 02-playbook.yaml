- name: "ginx install and start"
  hosts: catalogue
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present

    - name: Start nginx
      ansible.builtin.package:
        name: nginx
        state: started
        enabled: yes

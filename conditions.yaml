- name: Conditions
  hosts: local
  connection: local
  vars_prompt:
  - name: number
    prompt: provide number
    private: false
    type: int
  tasks:
  - name: condition check
    ansible.builtin.debug:
      msg: "Given number {{ number }} less than 10"
    when: number < 10

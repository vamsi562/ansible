- name: task level variables
  hosts: local
  connection: local
  vars:
    course: ansible
    tutor: siva
    from: "play level"
  tasks:
  - name: var test
    vars:
      course: devops
      tutor: cloud siva
      from: "task level"
    ansible.builtin.debug:
      msg: "Course is: {{ course }} and instructor is {{ tutor }}, from {{ from }}"
